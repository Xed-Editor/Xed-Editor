<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true" android:color="@color/berry" />
    <!-- Unfocused state -->
    <item android:color="@color/invert" />
</selector>